transforms:
  - !DeleteEnums
    from: ^(ISE|MCKOE|AFCNTR|CRC_)$
  - !RenameEnumVariants
    enum: ^BIDIOE$
    from: OutputDisabled
    to: Receive
  - !RenameEnumVariants
    enum: ^BIDIOE$
    from: OutputEnabled
    to: Transmit
